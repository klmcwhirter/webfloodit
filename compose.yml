services:
  ui:
    environment:
      TZ: PST8PDT
      EXPOSED_UI_PORT: 8080
    ports:
      - 8080:80
    build:
      context: .
      dockerfile: Containerfile
    deploy:
      replicas: 1
      restart_policy:
        condition: unless-stopped
